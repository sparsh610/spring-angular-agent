server:
  port: 8080

agent:
  provider: ${AGENT_PROVIDER:demo}
  max-steps: ${AGENT_MAX_STEPS:6}
  openai:
    api-key: ${OPENAI_API_KEY:}
    model: ${OPENAI_MODEL:gpt-4.1-mini}
    base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
